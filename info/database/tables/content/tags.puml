@startuml
object tags {
    App\Models\Content\Tag
    __
    + id
    - user_id (UBI)

    title (string)
    body (text)
    slug (string)
    rating (integer)
    total_posts (UI)
    total_views (UI)

    created_at
    updated_at
}
@enduml

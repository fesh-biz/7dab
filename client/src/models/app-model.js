import { Model } from '@vuex-orm/core'
// import { Router } from 'src/router'

export default class AppModel extends Model {
  // static beforeUpdate (newVal) {
  //   console.log('model fetched by: ', newVal.fetched_by)
  //
  //   console.log('this', this.query().find(newVal.id))
  //   console.log('model instance: ', this.fields())
  //
  //   console.log('router', Router.history.current.name)
  // }
}
